spring.application.name=backend_booking

# ===================== DATABASE =====================
spring.datasource.url=jdbc:mysql://localhost:3306/booking_reactnative?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================== JPA / HIBERNATE =====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===================== SERVER =====================
server.port=9999

# ===================== JWT =====================
app.jwt.secret=change_this_to_a_very_long_secret_key_at_least_32_chars_long_123456
app.jwt.expiration-ms=86400000

# ===================== EMAIL SMTP =====================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vubinh2005mt@gmail.com
spring.mail.password=hyvx spgg xyar gzpb
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===================== SWAGGER / SPRINGDOC =====================
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Link truy c?p Swagger - serve UI at /docs/index.html
springdoc.swagger-ui.path=/docs
# Let Swagger UI load the generated OpenAPI JSON
springdoc.swagger-ui.url=/v3/api-docs

# Ch? scan Controllers (tr�nh l?i qu�t to�n b? project)
springdoc.packages-to-scan=com.example.backend_booking.controller

# Using the default group; no explicit group override
